<!DOCTYPE HTML>
<html>

<head>
	<link rel="bookmark"  type="image/x-icon"  href="<%- config.root %><%- theme.head.favicon %>"/>
	<link rel="shortcut icon" href="<%- config.root %><%- theme.head.favicon %>">
	<% if(page.layout === 'gallery') { %>
		<%- partial('_partial/gallery_head', {}, {cache: true}) %>
	<% } else { %>
			<%- partial('_partial/head', {}, {cache: true}) %>
			<%- partial('_partial/importJS', {}, {cache: true}) %>
	<% } %>
</head>
    <% if(page.layout === 'gallery') { %>
        <!-- Single Gallery Page -->
        <%- partial('_widget/page-gallery', {}, {cache: true}) %>
        
    <% } else { %>
		<%- body %>
 	<% } %>
</html>
